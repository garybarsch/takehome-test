#
# Problem:
# Given an array of integers "nums" and an integer "target" value, return indices of
# the two numbers from the "nums" array such that they add up to "target".
# Return the answer in ascending sorted order.
#
# Example:
#     target = 3
#     nums = [2,1,4]
#     answer = [0, 1]
#     answer_sum = sum([nums[i] for i in answer])
#     is_correct = answer_sum == target
#
# TODO: implement two_sum()
#
# NOTE: simple/most common solution is O(n^2), best solution O(n)
#


def two_sum(*, nums: list[int], target: int) -> list[int]:
    """
    Args:
        nums (list[int]): haystack
        target (int): target value of adding two numbers from nums

    Returns:
       list[str]: A sorted list of indices from 'nums' that sum to the 'target' value.
    """
    # TODO: add your code here
    return []


def two_sum_optimized(*, nums: list[int], target: int) -> list[int]:
    """
    Optimized version of two_sum().

    Args:
        nums (list[int]): haystack
        target (int): target value of adding two numbers from nums

    Returns:
       list[str]: A sorted list of indices from 'nums' that sum to the 'target' value.
    """
    # TODO: add your code here (optional)
    return []
